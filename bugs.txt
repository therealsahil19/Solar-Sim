Description
Repeated iteration over labelGrid cells in nested loops causing potential performance degradation when many labels are present.

Location
src/managers/LabelManager.ts:305

Rationale
Optimizing inner loops is straightforward, e.g., by ensuring quick early exits or using better data structures.

Code context
            for (let c = startCol; c <= endCol; c++) {
                const idx = this.getGridIndex(c, r);
                if (idx === -1) continue;

                const cell = this.labelGrid[idx];
                if (!cell) continue;

                for (const other of cell) {
                    if (this.checkOverlap(item, other)) {
                        return true;

Description
The private validateSettings method handles crucial logic to ensure settings conform to the default schema types but is not explicitly tested.

Location
src/managers/SettingsManager.ts:106

Rationale
High confidence as we can use reflection or test it via the public `load()`/`set()` methods to verify settings validation logic.

Code context
    /**
     * Validates parsed settings against the default schema types.
     */
    private validateSettings(parsed: Record<string, unknown>): Partial<Settings> {
        const validated: Record<string, unknown> = {};
        for (const key of Object.keys(DEFAULT_SETTINGS) as SettingKey[]) {
            if (key in parsed && typeof parsed[key] === typeof DEFAULT_SETTINGS[key]) {
                validated[key] = parsed[key];
            }
        }

Description
A commented out snippet for adding a glow sprite exists in `src/procedural.ts` and appears to be dead code.

Location
src/procedural.ts:400

Rationale
Commented out code adds clutter and confusion. Removing it is safe.

Code context
    // Add Glow Sprite (already created above)
    // const glow = createGlowSprite(data.visual.radius, data.visual.color);
    // visualGroup.add(glow);


Description
The doc block for getOrbitalPosition contains duplicate descriptive sentences and numbered lists.

Location
src/physics.ts:23

Rationale
Fixing a JSDoc block is straightforward and zero risk.

Code context
 * Calculates the 3D position of a celestial body using Kepler's Laws.
 *
 * Logic:
 * 1. Calculates Mean Anomaly (M) based on time and orbital period.
 * Calculates the astronomical position of a body based on its orbital parameters.
 *
 * Technical Details:
 * 1. Mean Anomaly (M) is calculated from Time and Period.
 * 2. Eccentric Anomaly (E) is solved via Newton-Raphson iteration of Kepler's Equation: M = E - e*sin(E).

Description
The trailManager property on the Window interface is typed as 'any', circumventing TypeScript's type safety.

Location
src/types/index.ts:138

Rationale
The type 'TrailManager' is well defined and can simply be imported and used instead of 'any'. Requires a small, isolated change.

Code context
        trailManager: any; // Using any for simplicity in global scope for tests
        boltBenchmark?: {
            (durationMs?: number): {
Description
Multiple debug console.log statements are left in the benchmark.ts file, which clutter the console output.

Location
src/benchmark.ts:122

Rationale
Removing these console.log statements is safe and does not affect the actual logic. They are purely diagnostic.

Code context
    console.log('%c⚡ Bolt Benchmark Results', 'color: #ffc107; font-weight: bold; font-size: 14px;');
    console.log(`   Duration: ${(durationMs / 1000).toFixed(1)}s`);
    console.log(`   Frames:   ${stats.frames}`);

Description
The try/catch block inside `notifyListeners` catches errors thrown by listeners but throws them again; this should be verified with a test.

Location
src/managers/SettingsManager.ts:180

Rationale
High confidence. The test setup simply requires registering a mock listener that throws an error and asserting that the error propagates.

Code context
    private notifyListeners(key: SettingKey, value: SettingValue): void {
        for (const listener of this.listeners) {
            try {
                listener(key, value);
            } catch (e) {
                throw e;
            }
        }
    }

Description
The config object is cast to 'any' when passed to createBelt, which weakens type safety and can hide type mismatches.

Location
src/main.ts:222

Rationale
Requires updating the type signature of createBelt or ensuring 'config' conforms to the expected type, which is straightforward but might require minor type adjustments.

Code context
        chunk.forEach(config => {
            if (config.type === 'Belt') {
                const belt = createBelt(config as any);
                scene.add(belt);
                belts.push(belt);


Description
Math.random() is used to generate parameters for procedural debris generation. While acceptable for non-security visual tasks, it is a weak pseudo-random number generator and should be replaced with Crypto.getRandomValues() if any parameters ever influence security-sensitive application logic.

Location
src/debris.ts:292

Rationale
While the fix (switching to Crypto.getRandomValues()) is simple, it requires a small utility function to map the generated values to a [0, 1) range, which adds a few lines of code and minor overhead. However, it's a straightforward replacement.

Code context
    for (let i = 0; i < count; i++) {
        // Generate random Keplerian elements within the distribution bounds
        const a = distribution.minA + Math.random() * (distribution.maxA - distribution.minA);
        const e = distribution.minE + Math.random() * (distribution.maxE - distribution.minE);
        const inc = distribution.minI + Math.random() * (distribution.maxI - distribution.minI);

        const omega = Math.random() * 360;
        const Omega = Math.random() * 360;
        const M0 = Math.random() * 360;

Description
Iterating over every instance in instanced groups continuously during update cycles.

Location
src/instancing.ts:157

Rationale
Requires ensuring only dynamic or changed instances are processed.

Code context
            const forceUpdate = group.firstUpdatePending;
            let needsUpdate = forceUpdate;

            // Only iterate if we have dynamic instances or a fresh build
            for (let i = 0; i < group.instances.length; i++) {
                const instanceData = group.instances[i];
                if (!instanceData) continue;

                // Skip static instances unless it's the first update after build
                if (!instanceData.dynamic && !forceUpdate) continue;

Description
Double nested loop for updating trail segments can be optimized by flattening or utilizing typed arrays better.

Location
src/trails.ts:149

Rationale
Requires modifying how trails are stored and updated.

Code context
        let ptr = 0;
        for (let t = 0; t < this.maxTrails; t++) {
            for (let s = 0; s < this.segmentsPerTrail; s++) {
                // Vertex 0 (Start)
                indices[ptr] = s;
                vertexIndices[ptr] = 0;
                trailIndices[ptr] = t;
                ptr++;

                // Vertex 1 (End)

Description
Iterating through all animated objects linearly for physics updates, which scales poorly with large numbers of objects.

Location
src/main.ts:553

Rationale
Requires optimizing how animated objects are stored or processed, potentially requiring some restructuring.

Code context
    simulationTime += dt * SIMULATION_SPEED_BASE * timeScale;

    const len = animatedObjects.length;
    for (let i = 0; i < len; i++) {
        const obj = animatedObjects[i];
        if (!obj) continue;
        const physics = obj.physics;
        if (physics && physics.a !== undefined) {
            getOrbitalPosition(physics, simulationTime, _localPos);
            _worldPos.copy(_localPos);

Description
Not inferrable

Location
tests/unit/redundant_update.test.ts:132

Rationale
Matched regex for TODO/FIXME/etc in comments, but seems like test documentation rather than an actionable task.

Code context
        vi.mock('../../src/trails', () => ({
            TrailManager: class {
                constructor() { }
                update() { }
                flushRegistrations() { }
                dispose() { }
            }
        }));

        mainModule = await import('../../src/main');
    });

    afterEach(() => {
        vi.restoreAllMocks();
        mainModule?.dispose();
    });

    it('should have scene.matrixWorldAutoUpdate disabled', async () => {
        await mainModule.init();
        const scene = mainModule.scene;
        // Should be false after fix
        expect(scene.matrixWorldAutoUpdate).toBe(false);
    });

    it('should call updateMatrixWorld only once per frame', async () => {
        await mainModule.init();

        if (animateCallback) {
            sceneUpdateSpy.mockClear();
            animateCallback(performance.now());

            // Without fix: 2 calls (manual + renderer)

Description
The `init` function is overly long and does too many things (DOM manipulation, Three.js initialization, async fetching, event binding, lazy loading).

Location
src/main.ts:252

Rationale
Refactoring an initialization sequence involves decoupling multiple interdependent systems and carries risk of breaking the start sequence.

Code context
export async function init(): Promise<void> {
    interactionTargets.length = 0;
    animatedObjects.length = 0;
    planets.length = 0;
    allOrbits.length = 0;
    allTrails.length = 0;

    clearMaterialCache();

    initThreeJSEnvironment();


