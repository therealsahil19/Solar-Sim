easy

Description
The createStarfield function is exported but has no corresponding unit tests.

Location
src/procedural.ts:126

Rationale
The function simply creates and returns a THREE.Points object, which is easy to verify without complex setup.

Code context
    return Math.pow(a, 1.5);
}

/**
 * Creates the background starfield.
 */
export function createStarfield(): THREE.Points {
    const starsGeometry = new THREE.BufferGeometry();
    const starsMaterial = new THREE.PointsMaterial({

Description
Remove console.warn on localStorage read failure in SettingsManager.

Location
src/managers/SettingsManager.ts:99

Rationale
Clear fix (< 20 lines). Reduces console noise.

Code context
        try {
            const stored = localStorage.getItem(STORAGE_KEY);
            if (stored) {
                const parsed = JSON.parse(stored) as Record<string, unknown>;
                const validated = this.validateSettings(parsed);
                this.settings = { ...DEFAULT_SETTINGS, ...validated };
            }
        } catch (e) {
            console.warn('SettingsManager: Unable to load preferences.', e);
            this.settings = { ...DEFAULT_SETTINGS };
        }
    }

Description
Either throw an Error or ignore invalid keys quietly instead of using console.warn.

Location
src/managers/SettingsManager.ts:153

Rationale
Clear fix (< 20 lines). Throwing an error provides better development feedback.

Code context
     * @example settings.set('speed', 2.0);
     */
    set<K extends SettingKey>(key: K, value: Settings[K]): void {
        if (!(key in DEFAULT_SETTINGS)) {
            console.warn(`SettingsManager: Unknown setting "${key}"`);
            return;
        }

        this.settings[key] = value;
        this.save();
        this.notifyListeners(key, value);

Description
The error is logged via console.error and immediately thrown. The console.error is redundant.

Location
src/main.ts:297

Rationale
Clear fix (< 20 lines). Reduces log duplication.

Code context
        instanceRegistry!.groups.forEach(group => {
            if (group.mesh) interactionTargets.push(group.mesh);
        });
    } catch (error) {
        console.error("Failed to load system data:", error);
        initFailed = true;
        throw error;
    }

    setupInteractionsAndUI(planetData);

Description
Remove the commented out `allLabels.forEach(...)` as labels are now handled by LabelManager.

Location
src/main.ts:456

Rationale
Clear fix (< 20 lines). Removes confusing historical code.

Code context
    labelManager?.toggle(showLabels);
    // labelsNeedUpdate = true; // Handled by LabelManager
    const btn = document.getElementById('btn-labels');
    if (btn) btn.setAttribute('aria-pressed', String(showLabels));
    // allLabels.forEach(label => { label.visible = showLabels; });
    ToastManager.getInstance().show(`Labels: ${showLabels ? "ON" : "OFF"}`);
}

function toggleBelt(type: string, visible: boolean): void {
    belts.forEach(belt => {

Description
Silently clamp or expose configuration to handle maxTrails limits without spamming console.

Location
src/trails.ts:75

Rationale
Clear fix (< 20 lines). Cleans up expected runtime limits logging.

Code context
    constructor(scene: THREE.Scene, maxTrails: number = 5000, pointsPerTrail: number = 100) {
        this.scene = scene;
        // Clamp maxTrails to 4096 to ensure compatibility with most devices
        this.maxTrails = Math.min(maxTrails, 4096);
        if (maxTrails > 4096) {
            console.warn(`TrailManager: Clamping maxTrails from ${maxTrails} to ${this.maxTrails} for texture compatibility.`);
        }

        this.pointsPerTrail = pointsPerTrail;
        this.segmentsPerTrail = pointsPerTrail;

Description
Initialization failures in SettingsPanel should throw an Error to be caught by the application instead of using console.error.

Location
src/components/SettingsPanel.ts:87

Rationale
Clear fix (< 20 lines). Throwing an Error is a more robust way to handle missing required UI elements.

Code context
            toggleOortCloud: document.getElementById('setting-oort-cloud') as HTMLInputElement | null,
            themeButtons: document.querySelectorAll<HTMLButtonElement>('.theme-btn'),
            sliderSpeed: document.getElementById('setting-speed') as HTMLInputElement | null,
            speedValue: document.getElementById('setting-speed-value'),
        };

        if (!this.dom.panel) {
            console.error('SettingsPanel: #settings-panel not found in DOM.');
            return;
        }

        this.initializeFromSettings();

Description
If a Modal element is not a DIALOG tag, throw an Error instead of using console.error.

Location
src/components/Modal.ts:55

Rationale
Clear fix (< 20 lines). Validates preconditions explicitly by throwing.

Code context
            this.element = null;
            this.options = options;
            return;
        }

        if (el.tagName !== 'DIALOG') {
            console.error(`Modal: Element must be a <dialog> tag.`);
            this.element = null;
            this.options = options;
            return;
        }

        this.element = el as HTMLDialogElement;

Description
Consider catching and re-throwing errors or passing them to a global error handler instead of suppressing with console.error.

Location
src/managers/SettingsManager.ts:185

Rationale
Clear fix (< 20 lines). Helps surface listener errors directly to the application.

Code context
    private notifyListeners(key: SettingKey, value: SettingValue): void {
        for (const listener of this.listeners) {
            try {
                listener(key, value);
            } catch (e) {
                console.error('SettingsManager: Listener error', e);
            }
        }
    }

    /**

Description
Warn users gracefully via the UI or throw an error rather than logging to console when a CommandPalette item lacks a valid handler.

Location
src/components/CommandPalette.ts:441

Rationale
Clear fix (< 20 lines). Prevents silent failures in the command palette UI from the user's perspective.

Code context
    private executeCurrent(): void {
        const item = this.filteredItems[this.selectedIndex];
        if (item) {
            this.close();
            if (typeof item.handler === 'function') {
                item.handler();
            } else {
                console.warn(`CommandPalette: Item "${item.name}" has no valid handler.`);
            }
        }
    }
}

Description
Remove console.warn on localStorage write failure, as it is expected behavior in some privacy modes.

Location
src/managers/ThemeManager.ts:101

Rationale
Clear fix (< 20 lines). Reduces console noise.

Code context
    private setStoredTheme(themeName: string): void {
        try {
            localStorage.setItem('theme', themeName);
        } catch (e) {
            console.warn('ThemeManager: Unable to persist theme preference.', e);
        }
    }

    /**
     * Type guard to check if a string is a valid theme name.

Description
Remove console.warn on localStorage write failure in SettingsManager.

Location
src/managers/SettingsManager.ts:124

Rationale
Clear fix (< 20 lines). Reduces console noise.

Code context
    private save(): void {
        try {
            localStorage.setItem(STORAGE_KEY, JSON.stringify(this.settings));
        } catch (e) {
            console.warn('SettingsManager: Unable to save preferences.', e);
        }
    }

    /**
     * Retrieves the current value for a specific setting key.

Description
Invalid config URLs are gracefully handled; the console.warn can be removed or replaced with an application-level toast.

Location
src/main.ts:203

Rationale
Clear fix (< 20 lines). Removes unnecessary developer logs from production.

Code context
            configUrl = 'system.json';
            ToastManager.getInstance().show("External config blocked. Loaded default system.", { type: 'error' });
        }
    } catch (e) {
        console.warn('Invalid config URL:', configUrl);
        configUrl = 'system.json';
    }

    const response = await fetch(configUrl);
    if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);

Description
Remove the commented out `labelsNeedUpdate = true` code to clean up the source.

Location
src/main.ts:453

Rationale
Clear fix (< 20 lines). Cleans up obsolete code comments.

Code context
function toggleLabels(): void {
    showLabels = !showLabels;
    labelManager?.toggle(showLabels);
    // labelsNeedUpdate = true; // Handled by LabelManager
    const btn = document.getElementById('btn-labels');
    if (btn) btn.setAttribute('aria-pressed', String(showLabels));
    // allLabels.forEach(label => { label.visible = showLabels; });
    ToastManager.getInstance().show(`Labels: ${showLabels ? "ON" : "OFF"}`);
}

function toggleBelt(type: string, visible: boolean): void {

Description
The error is already being rendered into the DOM overlay. The console.error is unnecessary.

Location
src/main.ts:716

Rationale
Clear fix (< 20 lines). Consolidates error presentation logic.

Code context
    instanceRegistry = null;
    interactionHelpers?.dispose();
}

// Auto-initialize unless skipped (for testing)
if (!window.__SKIP_INIT__) {
    init().catch((err: Error) => {
        console.error("Initialization failed:", err);
        // Show error toast even if init fails
        ToastManager.getInstance().show(`Initialization Error: ${err.message}`, { type: 'error', duration: 10000 });

        // Fallback visual

Description
If a mesh is not found in input interaction, silently ignore or handle it instead of throwing warnings.

Location
src/input.ts:243

Rationale
Clear fix (< 20 lines). Users shouldn't see developer warnings for failed raycasts or missing items.

Code context
    function selectByName(name: string): void {
        const mesh = findMeshByName(name);
        if (mesh) {
            callbacks.onSetFocus(mesh);
            callbacks.onObjectSelected(mesh);
            updateSelectionUI(mesh);
        } else {
            console.warn(`Mesh not found for: ${name}`);
        }
    }

    return { updateSelectionUI, selectByName };

Description
Initialization failures in NavigationSidebar should throw an Error or be handled by an Error Boundary instead of silently logging to console.

Location
src/components/NavigationSidebar.ts:88

Rationale
Clear fix (< 20 lines). Throwing an Error allows the top-level application to catch and display a meaningful error state to the user.

Code context
        this.dom = {
            sidebar: document.getElementById('nav-sidebar'),
            list: document.getElementById('nav-list'),
            search: document.getElementById('nav-search') as HTMLInputElement | null,
            btnClose: document.getElementById('btn-close-nav') as HTMLButtonElement | null,
            btnOpen: document.getElementById('btn-planets') as HTMLButtonElement | null,
        };

        if (!this.dom.sidebar) {
            console.error('NavigationSidebar: #nav-sidebar not found in DOM.');
            return;
        }

        if (!this.dom.list) {
            console.warn('NavigationSidebar: #nav-list not found in DOM.');

Description
If a Modal element is not found, throw an Error instead of using console.error and returning silently.

Location
src/components/Modal.ts:48

Rationale
Clear fix (< 20 lines). Allows calling code to properly handle the failure.

Code context
        const el = typeof elementOrId === 'string'
            ? document.getElementById(elementOrId)
            : elementOrId;

        if (!el) {
            console.error(`Modal: Element not found.`);
            this.element = null;
            this.options = options;
            return;
        }

        if (el.tagName !== 'DIALOG') {

Description
Initialization failures in InfoPanel should throw an Error to be caught by the application instead of using console.error.

Location
src/components/InfoPanel.ts:73

Rationale
Clear fix (< 20 lines). Consistent error handling strategy across UI components.

Code context
            desc: document.getElementById('info-desc'),
            radius: document.getElementById('info-radius'),
            distance: document.getElementById('info-distance'),
            distSun: document.getElementById('info-dist-sun'),
            btnFollow: document.getElementById('btn-follow') as HTMLButtonElement | null,
        };

        if (!this.dom.panel) {
            console.error('InfoPanel: #info-panel not found in DOM.');
        }

        this.bindEvents();
    }

Description
Remove console.warn on localStorage read failure, as it is expected behavior in some privacy modes (e.g. Safari Private).

Location
src/managers/ThemeManager.ts:89

Rationale
Clear fix (< 20 lines). Reduces console noise in standard environments that restrict localStorage.

Code context
    private getStoredTheme(): string | null {
        try {
            return localStorage.getItem('theme');
        } catch (e) {
            console.warn('ThemeManager: Unable to read theme preference.', e);
            return null;
        }
    }

    /**

Description
In CommandPalette and NavigationSidebar, DOM elements are appended one by one in a loop, triggering layout thrashing.

Location
src/components/CommandPalette.ts:382

Rationale
Using a DocumentFragment allows appending all list items to the DOM in a single operation, which prevents multiple reflows/repaints.

Code context
                li.addEventListener('mouseenter', () => {
                    this.selectIndex(index, false);
                });

                this.list.appendChild(li);
            }

            // Update dynamic content
            li.id = `cmd-item-${index}`;

Description
The findMeshByName function iterates over the entire interactionTargets array to find a mesh by its name, which scales linearly O(N).

Location
src/input.ts:227

Rationale
For frequently accessed lookups, replacing the array `.find()` with a Map<string, THREE.Object3D> lookup changes the time complexity from O(N) to O(1).

Code context
    function findMeshByName(name: string): THREE.Object3D | null {
        const found = interactionTargets.find(obj => obj.userData.name === name);
        if (found) return found;

        if (context.instanceRegistry) {
            return context.instanceRegistry.findInstanceByName(name);
        }
        return null;
    }

Description
Using 'for...in' loops to delete properties from an object is generally slower than assigning a new empty object or using 'Map' if frequent clearing is needed.

Location
src/procedural.ts:74

Rationale
Deleting properties one-by-one is slow in V8 because it deoptimizes the object structure. While re-assigning requires changing to a 'let' variable, it's safer and faster. However, if 'delete' must be used, a Map is preferable.

Code context
export function clearMaterialCache(): void {
    Object.values(materialCache).forEach(mat => mat.dispose());
    for (const key in materialCache) delete materialCache[key];

    Object.values(textureMaterialCache).forEach(mat => mat.dispose());
    for (const key in textureMaterialCache) delete textureMaterialCache[key];

    Object.values(ringMaterialCache).forEach(mat => mat.dispose());
    for (const key in ringMaterialCache) delete ringMaterialCache[key];

Description
2 calls (manual + renderer)

Location
tests/unit/redundant_update.test.ts:142

Rationale
Matched regex for TODO/FIXME/etc in comments, but seems like test documentation rather than an actionable task.

Code context
        expect(scene.matrixWorldAutoUpdate).toBe(false);
    });

    it('should call updateMatrixWorld only once per frame', async () => {
        await mainModule.init();

        if (animateCallback) {
            sceneUpdateSpy.mockClear();
            animateCallback(performance.now());

            // Without fix: 2 calls (manual + renderer)
            // With fix: 1 call (manual)
            expect(sceneUpdateSpy).toHaveBeenCalledTimes(1);
        } else {
            throw new Error("animate callback was not registered");
        }
    });
});

Description
1 call (manual)

Location
tests/unit/redundant_update.test.ts:143

Rationale
Matched regex for TODO/FIXME/etc in comments, but seems like test documentation rather than an actionable task.

Code context
    });

    it('should call updateMatrixWorld only once per frame', async () => {
        await mainModule.init();

        if (animateCallback) {
            sceneUpdateSpy.mockClear();
            animateCallback(performance.now());

            // Without fix: 2 calls (manual + renderer)
            // With fix: 1 call (manual)
            expect(sceneUpdateSpy).toHaveBeenCalledTimes(1);
        } else {
            throw new Error("animate callback was not registered");
        }
    });
});

Description
Needs fix

Location
tests/unit/redundant_update.test.ts:131

Rationale
Matched regex for TODO/FIXME/etc in comments, but seems like test documentation rather than an actionable task.

Code context
    });

    afterEach(() => {
        vi.restoreAllMocks();
        mainModule?.dispose();
    });

    it('should have scene.matrixWorldAutoUpdate disabled', async () => {
        await mainModule.init();
        const scene = mainModule.scene;
        // Should be false after fix
        expect(scene.matrixWorldAutoUpdate).toBe(false);
    });

    it('should call updateMatrixWorld only once per frame', async () => {
        await mainModule.init();

        if (animateCallback) {
            sceneUpdateSpy.mockClear();
            animateCallback(performance.now());

